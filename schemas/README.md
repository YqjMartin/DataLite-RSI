# Manifest schemas

The JSON Schemas in this directory describe the public contribution formats.
They are intentionally small in the initial `0.1` version and may be extended
as the benchmark APIs stabilize.

The dependency-free validator in `scripts/validate_contributions.py` enforces
the repository-level conventions and important cross-references. JSON Schema
aware editors may use these files for completion and inline validation.

